<UserControl x:Class="PatrolControl.UI.Screens.UserManager.UserManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:PatrolControl.UI.Controls"
             mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource PanelWithShade}">
            <controls:GroupPanel Title="Add" >
                <HyperlinkButton x:Name="Add">Add</HyperlinkButton>
            </controls:GroupPanel>

            <controls:GroupPanel Title="Edit" >
                <ContentControl x:Name="ObjectEditor" ></ContentControl>
            </controls:GroupPanel>
        </Border>
    </Grid>
</UserControl>
